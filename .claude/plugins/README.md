# Claude Code Plugins (sentinel-managed)

Auto-generated plugin manifests live in this directory.

Each `<slug>.json` contains marketplace, install_cmds, priority_skills.

To activate: open this repo in Claude Code and run the install_cmds.
